# Rise Financial Services - Backend Configuration
# Copy this file to ".env" and adjust values as needed.
# Works out-of-the-box with SQLite (no setup needed) using the defaults below.

# Server port
PORT=8000

# Used for the SQLite database file (path is relative to the server folder)
SQLITE_PATH=data/applications.db
